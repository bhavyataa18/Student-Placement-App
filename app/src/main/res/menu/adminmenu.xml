<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">

        <item android:id="@+id/home"
            android:icon="@drawable/ic_home"
            android:title="Home"/>

        <item android:id="@+id/feeds"
            android:icon="@drawable/ic_feeds"
            android:title="New Feeds"/>

        <item android:id="@+id/sendnotification"
            android:icon="@drawable/ic_sendnotification"
            android:title="Send Nofication"/>

    </group>
    <group
        android:checkableBehavior="single">
        <item android:title="Sharing">
            <menu>
                <item
                    android:id="@+id/share"
                    android:icon="@drawable/ic_share"
                    android:title="Share"/>

            </menu>
        </item>
    </group>

    <group
        android:checkableBehavior="single">
        <item android:title="Authenticaton">
            <menu>
                <item
                    android:id="@+id/Logout"
                    android:icon="@drawable/ic_logout"
                    android:title="Logout"/>
            </menu>

        </item>

    </group>

</menu>